import { a as createEmotion } from './emotion-css.esm-4fec7074.js';

/**
 *  Create custom Emotion instance.
 */
const emotion = (el) => createEmotion({
  key: 'spx',
  container: el,
});

export { emotion as e };
